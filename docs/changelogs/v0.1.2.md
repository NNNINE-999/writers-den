# v0.1.2 · Bug 修复

**2026-05-01**

## 修复

- 中文用户名头像链接 404：Navbar 使用 `encodeURIComponent`，Profile 页使用 `decodeURIComponent`
- 中文标签 404：标签链接统一编码/解码
- 登录邮箱大小写敏感：`loginAction` 中 `email.toLowerCase()`
- 注册用户名加 `.trim()` 防不可见字符
- 一键复制按钮在 HTTP 下无效：改用 `document.execCommand('copy')` 降级方案
- 评论删除按钮无反馈：`startTransition` → `useTransition`，加 `isPending` loading 态 + 增大点击区
- 文章删除按钮无反馈：同上，确认按钮加 loading 态
